# 安全说明

## 凭据与部署数据

不要提交 API Key、插件连接 Token、Google Cookies、验证码服务密钥、代理密码、`config/setting.toml`、数据库或浏览器会话目录。截图应使用不透明遮挡，不能只靠模糊或覆盖一层可移除的图形。

首次启动前修改示例管理员密码。API Key 未配置时自动随机生成；已有数据库中的密钥不会被静默替换。管理服务应使用自己的 HTTPS 入口，限制不必要的公网访问，定期备份私有配置和数据库。

Docker 构建排除本地配置、会话、数据及隔离工作区，并保留 MIT 许可证。不要上传历史构建镜像：旧构建规则可能曾将本地配置包含在镜像层中。

## 报告问题

请通过维护者已有的私密联系渠道报告，不要在公开 Issue 粘贴凭据或原始数据库。可以提供脱敏后的错误码、接口路径、版本、状态码和复现步骤。

## 发布范围

公开仓库从服务端源码快照初始化，不携带内部 Git 历史、浏览器插件、产品端或账号管理工具。禁止直接镜像内部仓库、上传原始扫描报告及部署数据。历史清理不等于撤销凭据，凭据所有者需自行处理泄露风险。
